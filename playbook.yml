---
- name: Install NGINX stack on server
  hosts: localhost
  connection: local
  become: true

  tasks:
      - name: Install NGINX
        ansible.builtin.package:
          name: nginx
          state: latest

      - name: Start NGINX
        ansible.builtin.service:
          name: nginx
          state: started
          enabled: true
